# Baseline Snapshot — 2026-06-07

Pre-improvement run of the full quality gate. Used as the reference point for Phase 1–5 verification.

## Results

| Gate                 | Result                                                |
| -------------------- | ----------------------------------------------------- |
| `php artisan test`   | 14 passed, 45 assertions, 0.37s                       |
| `npm run type-check` | clean                                                 |
| `npm run build`      | clean, 16 chunks, 217 KB app entry gzipped to 75.6 KB |

## Test Inventory (14 tests, 45 assertions)

- `ApplicationUpTest::the_application_returns_a_successful_response` (1)
- `Auth/LoginControllerTest` (2): guest page, login with database token cookie
- `Auth/LogoutControllerTest` (1): user can logout
- `Auth/RegisterControllerTest` (2): guest page, register with database token cookie
- `DashboardControllerTest` (2): guest redirect, authenticated view
- `Settings/PasswordControllerTest` (2): view, update
- `Settings/ProfileControllerTest` (2): view, update
- `Thinkycz/LaravelCore/Guards/DatabaseTokenGuardTest` (2): local cookie attributes, non-local cookie attributes

## Gaps Carried Forward (from improvement plan)

- **Missing controllers with no tests:** `Web/Auth/ForgotPasswordController`, `Web/Auth/ResetPasswordController`, `Web/Auth/VerifyEmailController`
- **Missing unit/feature tests:** `User` model, `UserResource`, `HandleInertiaRequests`, `EnsureInertiaUserIsAuthenticated`, API controllers
- **No e2e suite:** `tests/e2e/` does not exist
- **No coverage target:** `make test-coverage` is not defined
- **No pre-commit hook:** `lefthook.yml` does not exist
- **No architecture diagram:** `docs/architecture.md` does not exist
- **No CHANGELOG, LICENSE, or CONTRIBUTING**

## Notes

- `phpunit.xml` has dead `PULSE_ENABLED`/`TELESCOPE_ENABLED` env entries (packages not installed).
- The double-hash bug exists in web + API auth controllers (Phase 1 fix).
- `tests/TestCase.php` reads `public/build/manifest.json` directly; will fail in fresh clones without `npm run build` first.
