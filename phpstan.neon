includes:
    - ./vendor/phpstan/phpstan/conf/bleedingEdge.neon
    # Note: the spaze/phpstan-disallowed-calls and phpstan-strict-rules
    # extension.neon files are auto-loaded by phpstan/extension-installer
    # (see composer.json require-dev). Listing them here as well would
    # double-load their rules and trigger a PHPStan warning. The
    # preset neons below set parameter values; the extensions themselves
    # are registered once by the installer.
    - ./vendor/spaze/phpstan-disallowed-calls/disallowed-dangerous-calls.neon
    - ./vendor/spaze/phpstan-disallowed-calls/disallowed-execution-calls.neon
    - ./vendor/spaze/phpstan-disallowed-calls/disallowed-insecure-calls.neon
    - ./vendor/spaze/phpstan-disallowed-calls/disallowed-loose-calls.neon

parameters:
  level: max
  checkMissingCallableSignature: true
  checkTooWideReturnTypesInProtectedAndPublicMethods: true
  checkUninitializedProperties: true
  reportAlwaysTrueInLastCondition: true
  checkBenevolentUnionTypes: true
  treatPhpDocTypesAsCertain: true
  strictRules:
    # Larastan models Eloquent's magic fluent builder calls through method
    # reflections that PHPStan strict-rules sees as static. Keep the rest of
    # strict-rules active, but do not report normal `$query->where()` /
    # `$query->forUser()` builder chains as dynamic static calls.
    dynamicCallOnStaticMethod: false
  paths:
    - ./app
    - ./config
    - ./database
    - ./resources
    - ./packages
    - ./routes
  excludePaths:
    - ./tests
